(*
 *
 * case lhs can have more than 1 element ONLY
 *    if the expression is a function
 *    if it is used within a function declaration
 *
 * tuples must have at least 2 elements, the left hand side must be an atom
 *
 * all lists are proper lists
 *
 *)

(*
 * all cases within a single fundecl must have the exact same arity
 *)

(*
 * type constructors must be qualified or unqualified atom names
 *)

(*
 * type parameters must be Var_name's
 *)

(*
 * make sure the right name variant is used everywhere
 *)

(*
 * make sure all the atoms are valid atom names
 *)

let check _ = ()
