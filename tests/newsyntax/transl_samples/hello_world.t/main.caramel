external format: string -> list<'a> -> unit = "io:format"

pub fn main(args) {
  match args {
  | [] -> format("~s\n", [:bye_bye])
  | [x, ...rest] ->
      format("Hello, ~s!\n", [ x ]);
      main(rest)
  }
}
