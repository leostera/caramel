(library
 (name caramel_compiler)
 (public_name caramel.compiler)
 (libraries compiler-libs.common caramel.core_erlang caramel.erlang))

(rule
  (targets compiler_version.ml)
  (deps (:input-file ../version.ml.in))
  (action (copy %{input-file} %{targets})))
